<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
  <title>Document</title>
</head>
<body class="bg-body text-body">
  <% // does existingPost exist? -- condition ? valueIfTrue : valueIfFalse %>
  <% const p = (typeof existingPost !== "undefined" && existingPost) ? existingPost : null; %>
  <div class="d-flex justify-content-center align-items-center vh-100">
    <form action="/post" method="POST" class="border rounded p-4" style="min-width: 400px;">
      <input type="hidden" name="id" value="<%= p ? p.id : '' %>">

      <div class="mb-3">
        <input
          name="subject"
          class="form-control"
          placeholder="Subject"
          value="<%= p ? p.subject : '' %>"
          required
        >
      </div>

      <div class="mb-3">
        <textarea
          name="content"
          class="form-control"
          rows="5"
          placeholder="Content"
          required
        ><%= p ? p.content : '' %></textarea>
      </div>

      <button type="submit" class="btn btn-primary w-100">
        <%= p ? 'Update Post' : 'Submit Post' %>
      </button>
    </form>
  </div>
</body>


</form>
</body>
</html>

